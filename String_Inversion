#python
class Solution:
    # @param A : string
    # @return a strings
    def solve(self, A):
        res = A.split()
        for ch in res:
            if ch.islower():
                ch = ch.upper()
            else:
                ch = ch.lower()
        return str(res)
