#python
class Solution:
    # @param A : string
    # @return an integer
    def solve(self, A):
        words = A.split()
        count = 0
        for word in words:
            if word == word[::-1]:
                count += 1
        return count
