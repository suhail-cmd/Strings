#python
class Solution:
    # @param A : string
    # @return a list of integers
    def solve(self, A):
        freq = [0] * 26
        for char in A:
            freq[ord(char) - ord('a')] += 1
        return freq
                
